import { type Config } from "prettier";

const config: Config = {
  endOfLine: "lf",
  printWidth: 100,
  quoteProps: "as-needed",
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "es5",
  useTabs: false,
  proseWrap: "always",
};

module.exports = config;
